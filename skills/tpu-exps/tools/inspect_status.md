Inspect the TPU experiment without changing state.

Run `tmux ls`, check recent logs under `~/tpu-exps/artifacts/logs/`, and summarize active sessions, latest metrics, and any obvious failures. Write `tpu-status.json` when the mission asks for a durable output.
