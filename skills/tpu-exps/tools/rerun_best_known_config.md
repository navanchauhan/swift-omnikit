Launch the best-known singing teacher-training rerun only after approval.

Use a detached tmux session and log to `artifacts/logs/teacher_unified_public_full_attention_singing_priority_rerun.log`. Verify startup with `tmux ls` and a short log tail.
